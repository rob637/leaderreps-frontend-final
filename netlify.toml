[build]
  # The directory containing the deployable application files
  publish = "dist" 
  # The command Netlify runs to build the project
  command = "npm run build"

[build.environment]
  # Pinning Node version to a specific, stable LTS release (v20) to satisfy Vite's dependency requirements.
  NODE_VERSION = "20.12.2"